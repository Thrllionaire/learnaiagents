# Lesson 01 — build notes

Scratch notes while working the build task. Anything surprising goes here; it feeds the
learning records.

- [ x] Loop typed out by hand (not pasted)
- [ x] Second tool added (`get_time`)
- [ x] Iteration counter + `stop_reason` printed each turn
- [x ] Asked a question needing both tools

      Did Claude request both in ONE response, or across two? → yes

- [ ] Broke it on purpose: appended `response.text` instead of `response.content`

      Exact error message → AttributeError: 'Message' object has no attribute 'text'

## Questions for the teacher
